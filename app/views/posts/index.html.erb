<div class="feed">

	<h1>Post Feed Page</h1>

	<% @posts.each do |post|%>
		<hr>
		<h2>
			<%= post.title%><br>
		</h2>
		<p>
			"<%= post.body%>"<br>	
		</p>
		<h4>
			- <%= User.find(post.user_id).username%>
		</h4>
	<% end %>

</div>
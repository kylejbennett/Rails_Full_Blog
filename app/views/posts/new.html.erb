<h1>Create a Post</h1>

<div class="post_form">

	<%= form_for Post.new do |f|%>
		<%= f.label :title%><br>
		<%= f.text_field :title%><br>
		<%= f.label :body%><br>
		<%= f.text_area :body%><br>
		<%= f.submit :create, data: {disable_with: "Submitting..."} %>
	<% end %>

</div>
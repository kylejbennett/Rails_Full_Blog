<div class="all_users">

	<h1>User's Index Page</h1>

	<% @users.each do |user|%>
		<div class="user">
			<p>
				<%= link_to user.username, user_path(user)%>
			</p>
			<%= image_tag user.avatar.url(:thumb) %>
		</div>
	<% end %>

	<div class="clear"></div>

</div>
	<h1>Change your password</h1>

	<%= form_for @user do |f| %>		
		<%= f.label "password" %><br>
		<%= f.password_field :password %><br>
		<br>
		<%= f.password_field :password_confirmation %><br>
		<%= f.submit :change %>
	<% end %>